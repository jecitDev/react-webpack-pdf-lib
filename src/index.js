import React from 'react';
import ReactDOM from 'react-dom';
import { PDFDocument, rgb } from "pdf-lib";

const App = () => <h1>Hello, React with Webpack 5!</h1>;

ReactDOM.render(<App />, document.getElementById('root'));
